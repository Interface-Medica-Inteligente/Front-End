// @flow
import * as React from 'react'
import './styles.css'

const Button = (): React.Node => {
    return(
        <button className='button'>
            Entrar
        </button>
    )
}

export default Button